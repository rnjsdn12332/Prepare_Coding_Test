def solution(info, edges):
    answer = 0
    visit=[False]*len(info)
    #해야
    
    def dfs(graph, x. visit) :
        visit[x]=True
        for i in gr
        if visit[x]==False
    return answer
